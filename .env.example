# Database
DB_URL=jdbc:postgresql://${host}:${port}/${table}
DB_USER=${user}
DB_PASSWORD=${password}

# LM Studio
LM_STUDIO_HOST=http://${host}:${port}
LM_STUDIO_TOKEN=${token}

# Telegram Bot
BOT_TOKEN=${token}
SERVER_URL=http://${host}:${port}
