server.port=8080

spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5433/mafia_game}
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASSWORD:1234}
spring.datasource.driver-class-name=org.postgresql.Driver

lmstudio.host=${LM_STUDIO_HOST:http://localhost:1234}
lmstudio.token=${LM_STUDIO_TOKEN:}
